namespace MasterIdiomas.Enums
{
    // Enum que define o status de um item
    public enum StatusEnum
    {
        // Status inativo
        Inativo = 0,

        // Status ativo
        Ativo = 1
    }
}